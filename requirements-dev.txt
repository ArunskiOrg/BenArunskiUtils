# Exact development tool versions. CI and the documented local setup both install
# this file, so a lint or test result reproduces across machines and an upstream
# release changes the build only when this file changes.
#
# pytest 9.0 raised its floor to Python 3.10, so the 8.4.x line is where pytest stays
# while Python 3.9 is the interpreter floor this project targets and the version CI
# runs. 8.4.2 is the newest release on that line.
pytest==8.4.2
ruff==0.16.2
pyyaml==6.0.3
